[project]
name = "education-finance-mlops"
version = "0.2.0"
requires-python = ">=3.10"
dependencies = ["pyarrow>=15,<22"]

[project.optional-dependencies]
release = ["kagglehub>=0.3,<2"]

[project.scripts]
education-finance-mlops = "education_finance_mlops.__main__:main"

[tool.setuptools.packages.find]
include = ["education_finance_mlops*"]
